// we can access the pwd function and store it in a variable like so
const pwd = require('./pwd');
const ls = require('./ls');
const cat = require(('./cat fileName').split(' '));



// if a user enters 'pwd' as an argument, we can then call it
pwd();


